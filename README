===
R2R
===

RST to RDF

run app:

 $ cd <path_to_r2r_dir>/
 $ python rst-service.py

To parse a file do:

curl -X POST -F "input=@<plain_file>" -F "doc=<doc_number> -F "ns=<namespace>" http://localhost:5050 

doc and ns parameters are optional:
default are doc=1, namespace=https://w3id.org/stlab/fred/rst/data/



==========
ATTENTION:

- now you need Feng and Converter services up 
- app is in virtual environment. To activate it do:
 $ cd <path_to_r2r_dir>/
 $ . rst-service/bin/activate
 


